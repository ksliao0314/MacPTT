{
  "name": "pttchrome-v3-tauri",
  "private": true,
  "version": "1.2.1",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build:frontend": "npm --prefix frontend run build && node scripts/build-frontend.cjs",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
